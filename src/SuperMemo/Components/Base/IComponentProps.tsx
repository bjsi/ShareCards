import DisplayAt from "./DisplayAt";
import DisplayState from "../../Elements/Base/DisplayState";

interface IComponentProps {
  displayAt: DisplayAt;
  displayState: DisplayState;
  height?: number;
  width?: number;
}

export default IComponentProps;
